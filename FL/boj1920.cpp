#include <stdio.h>
#include <string.h>
#include <vector>
#include <pair>

#define MAX_NUM 100001
using namespace std;

int main(int argc, char* argv[]){

    //make hash table
    int n = 0, m = 0;
    scanf("%d", &n);

    vector<pair<int, int>> hash_table;

    char* input;
    scanf("%s", input);


    //get keys
    scanf("%d", &n);


    //output
    return 0;
}

